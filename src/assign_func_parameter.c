/*
** *****************************************************************************
** yann.lebib
** yann.lebib <yann.lebib@stu003.cst.bunny.local>
**
** assign_func_parameter - 2022
**
** *****************************************************************************
*/

/*
t_symbol_table		*std_assign_func_parameter(char			*cmd,
						   size_t		*i,
						   t_symbol_table	*table)
{
  t_symbol_node		*step;
  
  if (std_read_text(cmd, i, "(") == false)
    return (NULL);
  step = table->table;
  while (std_read_text(cmd, i, ")") == false || step != NULL)
    {
      std_compute(cmd, i, tab)
      std_read_text(cmd, i ",");
      step = step->next;
    }
}

*/
